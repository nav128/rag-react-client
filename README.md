### Setup
- `git clone git@github.com:nav128/rag-react-client.git`
- `cd rag`
- create `.env` file and populate the fields as in the `env_example` file
- npm install .
- npm start

### Usage
- Upload files
- Chat
- And so on, And so fourth

Happy Raging No Dragging :)
